{% extends '@App/Layout/layout-two-col-4-8.html.twig' %}

{% block regionRight %}
    <div class="block block-term-view">
        <div class="block-content">
            <h2 class="field field-title">{{ term.name }}</h2>
            <div class="field field-title">{{ term.description }}</div>
            <div class="field field-created">{{ term.created | date('d.m.Y') }}</div>
        </div>
    </div>
{% endblock %}